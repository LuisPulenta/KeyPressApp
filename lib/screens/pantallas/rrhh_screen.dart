import 'package:flutter/material.dart';

class RrhhScreen extends StatelessWidget {
  const RrhhScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RR.HH.'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('RR.HH.'),
      ),
    );
  }
}
