import 'package:flutter/material.dart';

class FlotaKmPreventivoScreen extends StatelessWidget {
  const FlotaKmPreventivoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Km. y Preventivos'), centerTitle: true),
      body: const Center(child: Text('Km. y Preventivos')),
    );
  }
}
